<p style="color: green"><%= notice %></p>

<h1>Carts</h1>

<div id="carts">
  <% @carts.each do |cart| %>
    <%= render cart %>
  <% end %>
</div>
<%= link_to "Home", root_path %>

<%= link_to "New cart", new_cart_path %>
